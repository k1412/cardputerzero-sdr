## What changed

Describe the Cardputer Zero use case and the physical-key flow.

## Verification

- [ ] `ctest --test-dir build/linux-x86-64 -C Debug --output-on-failure`
- [ ] `scripts/smoke_locales.sh` for UI or translation changes
- [ ] Native 320×170 screenshot attached for UI changes
- [ ] ARM64 package workflow considered for build/packaging changes
- [ ] Physical Cardputer Zero evidence attached, or explicitly marked not run
- [ ] New source files include an SPDX license identifier
- [ ] No credentials, dongle serials, unrelated USB traces, or fetched/build trees are committed

## Hardware status

State the Cardputer Zero OS image, dongle/tuner, power arrangement, and checks run. Write `Not run` when this change has host-side evidence only.
